venv/
__pycache__/
*.pyc
.env

# База данных НЕ хранится в git: иначе при каждом передеплое хостинг
# затирал бы её версией из репозитория вместе со всем прогрессом.
# Бот создаёт и наполняет базу сам при первом запуске (см. ensure_seeded).
english_bot.db
*.db

bot_log.txt
seed_log.txt
cmudict.cache

# Модель распознавания речи (~70 МБ) — бот качает её сам при первом
# обращении к разделу произношения, в репозитории ей не место.
models/
_test_seed.db
