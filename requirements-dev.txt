# Зависимости, нужные ТОЛЬКО для пересборки словаря на своём компьютере
# (generate_wordlist.py, пересчёт частей речи). Боту на сервере они не нужны —
# он работает на requirements.txt и готовых JSON-файлах.
-r requirements.txt
wordfreq==3.1.1
nltk==3.9.1
requests==2.32.3
